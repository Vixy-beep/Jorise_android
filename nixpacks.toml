[phases.setup]
nixPkgs = ["python313"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["python manage.py collectstatic --no-input", "python manage.py migrate"]

[start]
cmd = "gunicorn jorise.wsgi:application --bind 0.0.0.0:$PORT --workers 4 --timeout 120"
